package chapter1_3;

public class Ex27 {
    /* 链表
    public int max(Node node) {
        if (node == null) {
            return 0;
        }
        if (node.next == null) {
            return node.item;
        }
        //
        if (node.item < node.next.item) {
            return max(node.next);
        } else {
            node.next = node.next.next;
            return max(node);
        }
    }*/
}

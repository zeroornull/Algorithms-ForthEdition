package chapter1_3;

public class Ex26 {

   /* 链表
    public Node remove(Item key) {

        while (first != null && first.item == key) {
            //
            first = first.next;
        }

        if (first == null) {
            return null;
        }

        Node cur = first, next = cur.next;
        while (next != null) {
            if (next.item = key) {
                cur.next = cur.next.next;
                next = cur.next;
            } else {
                // 指针后移
                cur = next;
                next = next.next;
            }
        }
        return first;

    }*/

}

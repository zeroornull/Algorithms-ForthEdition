package chapter1_3;

import jdk.nashorn.internal.ir.Node;

public class Ex21 {
    public static void main(String[] args) {
        Node x = null;
        String key = "test";
//        boolean b = find(x, key);
    }

    /* 链表
    private static boolean find(Node x, String key) {
        for (Node current = x; current != null; current = current.next) {
            if (current.item == key) {
                return true;
            }
        }
        return false;
    }*/
}
